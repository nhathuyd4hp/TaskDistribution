import "package:fluent_ui/fluent_ui.dart";

class ScheduleManagement extends StatelessWidget {
  const ScheduleManagement({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [Text("Schedule")]);
  }
}
