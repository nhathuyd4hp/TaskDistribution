import "package:fluent_ui/fluent_ui.dart";

class RobotManagement extends StatelessWidget {
  const RobotManagement({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [Text("Robot")]);
  }
}
